import React from "react";
import { render, screen } from "@testing-library/react";
import CCTable from './CCTable';

test("renders CCTable", () => {
  // render(<App />);
  // const linkElement = screen.getByText(/CCTable/i);
  // expect(linkElement).toBeInTheDocument();
});
